name: Build and Push pgvector Image (Legacy Disabled)

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Block legacy workflow
        run: |
          echo "This legacy pgvector workflow is disabled."
          echo "Database image builds are not part of the current ECS deploy path."
          exit 1
