name: Web Amplify Trigger (Legacy Disabled)

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Block legacy workflow
        run: |
          echo "This legacy Amplify workflow is disabled."
          echo "Web deploys go through ECS via .github/workflows/docker-ecr.yml."
          exit 1
