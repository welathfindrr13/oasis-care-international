name: Deploy Oasis Care to Staging (Legacy Disabled)

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Block legacy workflow
        run: |
          echo "This legacy workflow is disabled by design."
          echo "Use 'Deploy Docker Images to Staging' (.github/workflows/docker-ecr.yml)."
          exit 1
