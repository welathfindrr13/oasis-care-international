name: Diagnose Auth & Permissions (Legacy Disabled)

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Block legacy workflow
        run: |
          echo "This diagnostic workflow is disabled."
          echo "Use '.github/workflows/aws-check.yml' for current deploy auth validation."
          exit 1
