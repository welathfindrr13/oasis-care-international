name: Figma Design System Sync (Legacy Disabled)

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Block legacy workflow
        run: |
          echo "This legacy Figma sync workflow is disabled."
          echo "It is intentionally kept inert to avoid CI noise and accidental runs."
          exit 1
